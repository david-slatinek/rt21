from __init__ import SignCollection, DriveCollection, LocationCollection
from bson.objectid import ObjectId

if __name__ == "__main__":
    # data = SignCollection.find({})
    # for document in data:
    # SignCollection.update_one({'_id': document["_id"]}, {'$set': {"longitude": float(document["longitude"])}})
    # print(document)

    # _id = '60c31ac34ddcfce11ddf6124'
    # DriveCollection.delete_one({'_id': ObjectId(_id)})
    #
    # LocationCollection.delete_many({'drive_id': _id})
    # SignCollection.delete_many({'drive_id': _id})
    print()
